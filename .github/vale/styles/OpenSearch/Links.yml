extends: existence
message: "Add a trailing slash to the link '%s'."
level: error
tokens:
  - '\]\(\{\{site.url\}\}\{\{site.baseurl\}\}(?:(.*))?([^\/])\)'
  - '\]\(((http(s)?):\/\/)?(?:(.*))?([^\/])\)'
